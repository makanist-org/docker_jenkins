# Dockerfile: jenkins-python-agent
FROM python:3.11-slim

RUN apt-get update && apt-get install -y git curl

# Optional: create jenkins user
RUN useradd -m -s /bin/bash jenkins
USER jenkins
WORKDIR /home/jenkins